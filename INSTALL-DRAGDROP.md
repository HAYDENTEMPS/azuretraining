# Install Drag and Drop Dependency

Run this command in your terminal:

```bash
npm install @hello-pangea/dnd
```

Or if you use yarn:

```bash
yarn add @hello-pangea/dnd
```

This package is required for the drag-and-drop ordering questions feature.
